╔══════════════════════════════════════════════════════════╗
║     Deploy Step-by-Step - Copy & Paste ได้เลย!         ║
╚══════════════════════════════════════════════════════════╝

Server: 47.129.144.109
User: bitnami

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 1: อัพโหลดจาก Mac
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

เปิด Terminal บน Mac แล้ว copy-paste คำสั่งนี้:

cd /Users/Macbook
tar -czf bot-manager.tar.gz --exclude='Bot_Manager/node_modules' --exclude='Bot_Manager/server/data' --exclude='Bot_Manager/.git' Bot_Manager/
scp bot-manager.tar.gz bitnami@47.129.144.109:/tmp/

(จะถามรหัสผ่าน - ใส่รหัสที่มี)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 2: SSH เข้า Server
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ssh bitnami@47.129.144.109

(ใส่รหัสผ่าน)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 3: Extract Project
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Copy-paste คำสั่งนี้ (ทั้งหมดพร้อมกัน):

sudo mkdir -p /opt/bot-manager
sudo chown -R bitnami:bitnami /opt/bot-manager
cd /opt/bot-manager
tar -xzf /tmp/bot-manager.tar.gz --strip-components=1
rm /tmp/bot-manager.tar.gz
ls -la

(ต้องเห็นไฟล์ deploy/, server/, client/, etc.)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 4: Run Auto Setup
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

bash deploy/auto-setup.sh

จะถาม 2 คำถาม:
1. VPS IP: พิมพ์ 47.129.144.109
2. Domain: กด Enter ข้าม

รอ 5-10 นาที... ☕

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 5: เสร็จแล้ว! 🎉
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

เปิดเบราว์เซอร์:
http://47.129.144.109

Login:
Username: admin
Password: admin123

⚠️ เปลี่ยนรหัสผ่านทันที!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
คำสั่งเช็คสถานะ (บน Server)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

pm2 status                      # เช็คว่ารันไหม
pm2 logs bot-manager-api        # ดู logs
bash deploy/check-resources.sh  # เช็ค RAM/CPU
pm2 restart bot-manager-api     # รีสตาร์ท

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
สร้าง Bot แรก
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. เปิด http://47.129.144.109
2. Login: admin / admin123
3. คลิก "+ Create Price Collector"
4. กรอก:
   Name: BTC Spot Collector
   Category: Collector
   Symbol: btcusdc
   Socket Type: Spot
5. Create → Start

✅ เสร็จ! Bot เริ่มเก็บข้อมูลแล้ว 🤖

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

หมายเหตุ:
- ทุกคำสั่ง copy-paste ได้เลย
- ถ้าเจอ error บอกได้
- Script จะทำให้อัตโนมัติทั้งหมด
