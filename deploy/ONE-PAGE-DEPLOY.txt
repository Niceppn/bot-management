╔══════════════════════════════════════════════════════════╗
║        Bot Manager - One Page Deploy Guide              ║
║        คำสั่งทั้งหมดอยู่ในหน้าเดียว!                    ║
╚══════════════════════════════════════════════════════════╝

📦 STEP 1: อัพโหลดโปรเจค (จาก Mac)
─────────────────────────────────────────────────────────
cd /Users/Macbook
tar -czf bot-manager.tar.gz \
    --exclude='Bot_Manager/node_modules' \
    --exclude='Bot_Manager/server/data' \
    --exclude='Bot_Manager/.git' \
    Bot_Manager/

scp -i ~/Downloads/LightsailKey.pem \
    bot-manager.tar.gz \
    ubuntu@YOUR_VPS_IP:/tmp/

🚀 STEP 2: Setup (บน VPS)
─────────────────────────────────────────────────────────
ssh -i ~/Downloads/LightsailKey.pem ubuntu@YOUR_VPS_IP

sudo mkdir -p /opt/bot-manager
sudo chown -R ubuntu:ubuntu /opt/bot-manager
cd /opt/bot-manager
tar -xzf /tmp/bot-manager.tar.gz --strip-components=1
rm /tmp/bot-manager.tar.gz

bash deploy/auto-setup.sh

  ↑ ตอบ 2 คำถาม:
    1. VPS IP
    2. Domain (กด Enter ข้ามได้)

  ↑ รอ 5-10 นาที

✅ DONE!
─────────────────────────────────────────────────────────
เปิด: http://YOUR_VPS_IP
Login: admin / admin123

📊 เช็คสถานะ
─────────────────────────────────────────────────────────
pm2 status
pm2 logs bot-manager-api
bash deploy/check-resources.sh

🔧 คำสั่งที่ใช้บ่อย
─────────────────────────────────────────────────────────
pm2 restart bot-manager-api    # Restart
pm2 logs                        # View logs
bash deploy/backup.sh           # Backup
bash deploy/check-resources.sh  # Check RAM/CPU/Disk

📝 สร้าง Bot แรก
─────────────────────────────────────────────────────────
1. เปิด http://YOUR_VPS_IP
2. Login: admin / admin123
3. คลิก "+ Create Price Collector"
4. กรอก:
   - Name: BTC Spot Collector
   - Category: Collector
   - Symbol: btcusdc
   - Socket: Spot
5. คลิก "Create Bot"
6. คลิก "▶ Start"

✅ เสร็จแล้ว! Bot เริ่มเก็บข้อมูล

💾 Backup
─────────────────────────────────────────────────────────
# Manual backup
cd /opt/bot-manager
tar -czf ~/backup-$(date +%Y%m%d).tar.gz \
  server/data server/logs server/.env

# Download to Mac
scp -i ~/Downloads/LightsailKey.pem \
  ubuntu@YOUR_IP:~/backup-*.tar.gz \
  ~/Downloads/

# Or: Lightsail Snapshot (แนะนำ)
Lightsail Console → Snapshots → Create snapshot

🐛 ถ้ามีปัญหา
─────────────────────────────────────────────────────────
pm2 logs bot-manager-api        # ดู error logs
sudo nginx -t                   # เช็ค nginx config
free -h                         # เช็ค RAM
df -h                          # เช็ค disk
sudo systemctl status nginx     # เช็ค nginx status

🎯 That's it!
─────────────────────────────────────────────────────────
